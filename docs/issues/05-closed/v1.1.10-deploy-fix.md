Ensure all bindings in wrangler.toml use this formatting and update documentation to ensure it stays that way:

[[kv_namespaces]]
binding = "..."
id = "..."

[[r2_buckets]]
binding = "..."
bucket_name = "..."
preview_bucket_name = "..."